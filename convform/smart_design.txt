definovat kdy (fallbacky, overiterated?)

prompt z tracků a tak podobně

kontext z databáze

// how to signal that its time to generate stuffs?
//
// langchain techniques -
// annotate
//      anotate convo based on example + context of current convo
// generate
//      based on annotated context and an annotation generate an answer
// third thing?


regex to go from the end until \n, ?, : or beginning of string and only return that
sanitize repeated phrases from prompt

"$" in keywords means only chgpt can tell, the annotation has to be the hint

contextual analysis
contextual awareness
entity memory
